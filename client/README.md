# Smart Inbox Engine – Client (React)

This is the **React frontend** for the Smart Inbox Engine.  
It allows users to load mock emails, send them to the .NET backend for scoring, and display the results sorted by priority with clear visual cues.

---

## Features

- Load predefined **mock email data**
- Send raw emails to the backend API for processing
- Display emails sorted by **Priority Score**
- Visual priority indicators:
  - 🔴 **High Priority** (> 70)
  - 🟢 **Low Priority** (< 30)
- Simple, clean UI
- Frontend unit tests included

---

## Prerequisites

- **Node.js** (v16+ recommended)
- **npm**

---

## Setup & Run

From the `client` directory:

### Install dependencies

```bash
npm install
```
